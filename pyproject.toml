[project]
name = "deep-dive-pyspark"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pyspark==4.1.1",
    "delta-spark==4.2.0",
]

[dependency-groups]
dev = [
    "pytest==9.0.3",
]

[tool.uv]
package = false
